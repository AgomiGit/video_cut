[project]
name = "auto-highlight-editor"
version = "0.1.0"
description = "Local-first automatic highlight editor"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
transcribe = [
  "faster-whisper>=1.1.0",
]

[tool.uv]
package = false
